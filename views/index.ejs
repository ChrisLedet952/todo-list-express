<!DOCTYPE html>
<!--telling browser doc type-->
<html lang="en">
  <!--starting the html tag saying that the lang is English-->
  <head>
    <!--start of head tag to store our metadata-->
    <meta charset="UTF-8" />
    <!--indicates what characters set should be used-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <!--change tab title-->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    />
    <link rel="stylesheet" href="css/style.css" />
  </head>
  <body>
    <!--main section for content displayed-->
    <!DOCTYPE html>
    <!--This might be an extra header typo-->
    <html lang="en">
      <!--This might be an extra header typo-->
      <head>
        <!--This might be an extra header typo-->
        <meta charset="UTF-8" />
        <!--This might be an extra header typo-->
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <!--This might be an extra header typo-->
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <!--This might be an extra header typo-->
        <title>Document</title>
        <!--This might be an extra header typo--></head
      ><!--This might be an extra header typo-->
      <body>
        <h1>Todo List:</h1>
        <!--main header displayed on page-->
        <ul class="todoItems">
          <!--unordered list for the todo list, assign class to the list-->
          <% for(let i=0; i < items.length; i++) {%><!--Use EJS to start a for loop-->
          <li class="item">
            <!--creating a list classed item-->
            <% if(items[i].completed === true) {%><!--EJS, creating an if statement to check if item in an array with the completed property is true-->
            <span class="completed"><%= items[i].thing %></span
            ><!--Creating an inline element with a class and adding EJS to display the name of the task-->
            <% }else{ %><!--EJS for our else clause of our if statement-->
            <span><%= items[i].thing %></span
            ><!--adding EJS to display the name of the task with NO CLASS-->
            <% } %>
            <!--EJS to close the ELSE STATEMENT-->
            <span class="fa fa-trash"></span>
            <!--Display FONT AWESOME ICON of trash can inside a span-->
          </li>
          <!--Closing of list item-->
          <% } %><!--EJS to close the FOR LOOP-->
        </ul>
        <!--closing the unordered list-->

        <h2>Left to do: <%= left %></h2>
        <!--creating a second level heading with text and also EJS of how many tasks are left, a variable-->

        <h2>Add A Todo:</h2>
        <!--Create a second level header-->

        <form action="/addTodo" method="POST">
          <!--creating a form with a POST methodthat goes to the /addTodo route-->
          <input
            type="text"
            placeholder="Thing To Do"
            name="todoItem"
          /><!--creating an input box where you can put text, placeholder text when the box is empty
        and names the input todoItem to give it a unique value-->
          <input type="submit" /><!-- Gives the form a submit button-->
        </form>
        <!--closing the form, you can use forms to post and submit things for the user to create. forms can post but they cant update-->

        <script src="js/main.js"></script>
        <!--connecting to our public/js/main.js file-->
      </body>
      <!--closing body-->
    </html>
    <!--closing html-->
  </body>
  <!--closing body-->
</html>
<!--closing html-->
